{
  "name": "singleton",
  "version": "1.0.0",
  "description": "JS singleton class",
  "main": "main.js",
  "scripts": {
    "start": "npm i && live-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.1"
  },
  "dependencies": {}
}
